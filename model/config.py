"""Variegata model configuration."""

import pathlib

# Root of this application, useful if it doesn't occupy an entire domain
APPLICATION_ROOT = '/'

VARIEGATA_MODEL_ROOT = pathlib.Path(__file__).resolve().parent
